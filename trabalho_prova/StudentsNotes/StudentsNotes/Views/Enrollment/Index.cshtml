@model IEnumerable<StudentsNotes.Models.Enrollment>

@{
    ViewData["Title"] = "Index";
}

<style>
    .card-container {
        display: flex;
        flex-wrap: wrap;
        gap: 20px;
        margin-bottom: 20px;
    }

    .card {
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        background-color: #f8f9fa;
        max-width: 400px;
        width: 100%;
    }

    .card-body {
        padding: 15px;
    }

    .btn-primary {
        display: inline-block;
        padding: 10px 20px;
        border-radius: 4px;
        background-color: #007bff;
        color: #fff;
        border: 1px solid #007bff;
        transition: background-color 0.3s ease, color 0.3s ease;
    }

        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #0056b3;
        }
</style>

<h1>Index</h1>

<div class="card-container">
    @foreach (var item in Model)
    {
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">@Html.DisplayFor(modelItem => item.EnrollmentYear)</h5>
                <p class="card-text">
                    <strong>Status:</strong> @Html.DisplayFor(modelItem => item.Status)<br />
                    <strong>Aluno:</strong> @Html.DisplayFor(modelItem => item.Student.StudentName)<br />
                </p>
                <a asp-action="Edit" asp-route-id="@item.EnrollmentID" class="btn btn-primary">Edit</a>
                <a asp-action="Details" asp-route-id="@item.EnrollmentID" class="btn btn-primary">Details</a>
                <a asp-action="Delete" asp-route-id="@item.EnrollmentID" class="btn btn-primary">Delete</a>
            </div>
        </div>
    }
</div>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>
